{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "$id": "./o3de-template-2.0.0.json",
    "title": "O3DE Template Schema 2.0.0",
    "description": "Schema 2.0.0 for O3DE templates.",
    "type": "object",
    "properties": {
        "$schema": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/$schema"
        },
        "$schemaVersion": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/$schemaVersion"
        },
        "template": {
            "description": "The template header.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/objectHeader"
        },
        "origin": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/origin"
        },
        "licenses": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/licenses"
        },
        "children": {
            "description": "List all direct child O3DE objects relative paths here.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/children"
        },
        "remote": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/remote"
        },
        "dependent": {
            "description": "List the names and optionally version(s) of objects this object depends on. A dependency means if this object is not present this object will fail and that this dependent object should be processed before this object is processed or it will fail to find dependent targets.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/objectNameAndVersionLists"
        },
        "optional_dependent": {
            "description": "List the names and optionally version(s) of objects this object can optionally use. These dependencies enhance functionality but are not required. The object will still work without them.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/objectNameAndVersionLists"
        },
        "peer_dependent": {
            "description": "List the names and optionally version(s) of objects that must also be present when a consumer uses this object. Unlike dependencies which this object itself needs, peer dependencies are requirements imposed on the consumer.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/objectNameAndVersionLists"
        },
        "hooks": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/hooks"
        },
        "icon": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/icon"
        },
        "documentation": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/documentation"
        },
        "canonical_tags": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/canonical_tags"
        },
        "user_tags": {
            "description": "These are non canonical tags, defined by the originator (creator), exclusive of the canonical tags, that Project Manager uses to search, sort and filter objects. These can be anything you want to tag your object with i.e. 'AI' or 'Space' or 'Human' etc.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/user_tags"
        },
        "platforms": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/platforms"
        },
        "requirements": {
            "type": "string",
            "description": "If you have extra instructions for the user, beyond the normal O3DE setup, on how to setup/use this object. i.e. 'You must install XYZ SDK 1.2 to compile this object.', or 'Read Setup.md before compiling."
        },
        "downloads": {
            "description": "If this object is available as a zip download such as a release. It can have a source and lfs portion.",
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/downloads"
        },
        "source_control": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/source_control"
        },
        "releases": {
            "$ref": "./o3de-patterns-2.0.0.json#/definitions/releases"
        },
        "copyFiles": {
            "type": "array",
            "description": "List all files that need to be copied from this object to instance the template.",
            "items": {
                "type": "object",
                "properties": {
                    "file": {
                        "description": "The relative path to the file from this object's root. i.e. 'Code/MyFile.cpp'",
                        "$ref": "./o3de-patterns-2.0.0.json#/definitions/relativePath"
                    },
                    "isTemplated": {
                        "type": "boolean",
                        "description": "If this file a templated file that needs to be processed by the O3DE templating system? If so then set this to true. If false, then this file will only be copied and the processing step will be skipped by the templating system, resulting in an instancing speed increase. This is a optimization only; if the file does not contain any tags to be processed and this is set to be true anyway, it is not an error and will still work, but it will be slower. Since there can be thousands of files in a template properly setting this flag will result in a significant speed increase",
                        "default": true
                    }
                },
                "required": [
                    "file",
                    "isTemplated"
                ]
            }
        },
        "createDirectories": {
            "type": "array",
            "description": "List all directories that need to be created to instance the template.",
            "items": {
                "description": "The relative path to the directory from this object's root. i.e. 'Code/MyDirectory'",
                "$ref": "./o3de-patterns-2.0.0.json#/definitions/relativePath"
            }
        }
    },
    "required": [
        "$schema",
        "$schemaVersion",
        "template",
        "origin",
        "licenses",
        "copyFiles",
        "createDirectories"
    ]
}